# Node / Vite
web/node_modules/
web/dist/
web/.env
web/.env.local
web/.env.*.local

# Python
jobs/__pycache__/
jobs/.venv/
jobs/.env
# clé service_role pour les runs locaux (ne PAS committer)
jobs/.mpv-secrets*
jobs/snap_*.json        # captures locales (--save-snapshot) ; les fixtures sont dans tests/fixtures
*.pyc

# Divers
.DS_Store
/public/        # ancien artefact Pages à la racine (pas web/public/ de Vite)
*.log
